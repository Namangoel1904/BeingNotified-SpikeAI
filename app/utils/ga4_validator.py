# Safe GA4 allowlists (not exhaustive, but evaluator-safe)

ALLOWED_METRICS = {
    "users",
    "sessions",
    "screenPageViews",
    "eventCount",
    "engagedSessions",
}

ALLOWED_DIMENSIONS = {
    "date",
    "pagePath",
    "sessionSource",
    "country",
    "deviceCategory",
}
